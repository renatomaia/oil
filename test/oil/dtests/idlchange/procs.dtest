local OIL_HOME = assert(os.getenv("OIL_HOME"),
	"OIL_HOME environment variable not defined")

execpath = OIL_HOME.."/test/oil/dtests/idlchange/";
services = {
	Server = { command = "lua -l oil.compat server.lua" };
};
tasks = {
	Tester = { dtestscript = "tester.lua" };
};
